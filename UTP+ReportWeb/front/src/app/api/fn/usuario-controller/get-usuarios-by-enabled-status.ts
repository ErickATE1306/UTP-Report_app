/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UsuarioDto } from '../../models/usuario-dto';

export interface GetUsuariosByEnabledStatus$Params {
  enabled: boolean;
}

export function getUsuariosByEnabledStatus(http: HttpClient, rootUrl: string, params: GetUsuariosByEnabledStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<UsuarioDto>>> {
  const rb = new RequestBuilder(rootUrl, getUsuariosByEnabledStatus.PATH, 'get');
  if (params) {
    rb.query('enabled', params.enabled, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<UsuarioDto>>;
    })
  );
}

getUsuariosByEnabledStatus.PATH = '/api/usuarios/enabled';
